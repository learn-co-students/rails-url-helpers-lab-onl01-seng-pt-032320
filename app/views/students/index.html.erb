<div>
  <% @students.each do |student| %>
    <div><%= link_to student.to_s, post_path(student) %></div>
    <div><%= link_to "student", student.to_s, post_path(student) %></div>
    
  <% end %>
</div>